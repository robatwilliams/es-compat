{
  "name": "es-compat",
  "private": true,
  "scripts": {
    "check:lint": "eslint --no-eslintrc --config=.eslintrc.json **/*.js",
    "check:prettier": "prettier --check **/*.{js,json,md}"
  },
  "devDependencies": {
    "eslint": "^6.7.2",
    "lerna": "^3.19.0",
    "prettier": "^1.19.1"
  }
}
