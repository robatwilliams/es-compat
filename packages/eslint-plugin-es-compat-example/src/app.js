/**
 * ES2019
 */
foo.flat();
residentialAddress.flat; // no error please
foo.flatMap();

const u2028 = ' '; // line separator character \u2028 is in this string

Object.fromEntries();

try {
} catch {}

foo.trim();

/**
 * ES2018
 */
const foo = { ...bar };
